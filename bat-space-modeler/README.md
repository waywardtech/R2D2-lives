# Bat-Space Modeler scaffold

Phase 0 contains only the generic `MobileAgentClient` boundary and a seeded
`SimMobileAgentClient`. Acoustic solver work is intentionally absent until the
R2 MVP gate. The skeleton has no import or runtime dependency on R2 Runtime.


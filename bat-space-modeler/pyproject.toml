[project]
name = "bat-space-modeler"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = ["sap-protocol==0.1.0"]

[build-system]
requires = ["setuptools==75.8.0"]
build-backend = "setuptools.build_meta"

